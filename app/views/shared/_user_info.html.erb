<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "내 정보 보기", current_user %></span>
<span><%= "#{current_user.nanoposts.count}개의 글" %></span>