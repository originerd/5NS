<li id="nanopost-<%= nanopost.id %>">
  <%= link_to gravatar_for(nanopost.user, size: 50), nanopost.user %>
  <span class="user"><%= link_to nanopost.user.name, nanopost.user %></span>
  <span class="content">
    <%= nanopost.content %>
    <% if nanopost.likes.find_by(user: current_user).present? %>
      <%= link_to "<span class=\"glyphicon glyphicon-thumbs-up\" aria-hidden=\"true\"></span> #{nanopost.likes.count}".html_safe, nanopost_likes_path(nanopost), method: :delete, class: "btn btn-primary btn-xs" %>
    <% else %>
      <%= link_to "<span class=\"glyphicon glyphicon-thumbs-up\" aria-hidden=\"true\"></span> #{nanopost.likes.count}".html_safe, nanopost_likes_path(nanopost), method: :create, class: "btn btn-default btn-xs" %>
      </a>
    <% end %>
    <% if nanopost.picture? %>
      <span class="content-image">
        <%= link_to nanopost.picture.url, class: "image-link" do %>
          <%= image_tag nanopost.picture.url, class: "img-responsive" %>
        <% end %>
      </span>
    <% end %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(nanopost.created_at) %> ago.
    <% if current_user == nanopost.user %>
      <%= link_to "delete", nanopost, method: :delete,
                                      data: { confirm: "정말 삭제하시겠습니까?" } %>
    <% end %>
  </span>
</li>